export const publicRoutes = [
  "/",
  "/search/*",
  "/video/*",
  "/user/*",
  "/api/*",
  "/refresh/*",
];

export const authRoutes = ["/auth/user"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/";
